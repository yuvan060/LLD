package splitwise.util;

import splitwise.entity.*;
import java.util.*;

public class DebtSimplifier {

    /**
     * Simplify debts among a set of users
     * @param splits list of unpaid splits between users
     * @return list of minimal transactions needed to settle all debts
     */
    public List<Transaction> simplifyDebts(List<Split> splits) {
        // TODO: Implement
        List<Transaction> minimalTransactions = new ArrayList<>();
        return minimalTransactions;
    }

}

