package behavioural.stratergy;

public class Strategy {
    /*
    Strategy design pattern allows the client code to send the functionality we want to perform,
    let's say we have payment method, where our client may perform different methodologies like card payments, upi payments
    cash payment. so instead of handling the cases with if else or switch cases, we can allow the client to send the strategy we want to perform
    which is more feasible and scalable so that introducing new strategy doesn't affect the current code.
     */
}
