package behavioural.command;

public class Command {
    /*
    The command design pattern is treat the set of instructions as objects, then call the execute method whenever it is needed.
    Let's say we have scheduler, we want execute the instructions on demand, so just wrap those functionalities inside an object and play with it.
     */
}
