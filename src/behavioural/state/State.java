package behavioural.state;

public class State {
    /*
    When the object has multiple states to deal with,
    instead of handling state transition and state behavioural functionalities in if else or switch,
    create a state interface and implement the states with required contracts and handle only the state transition of particular state.
     */
}
