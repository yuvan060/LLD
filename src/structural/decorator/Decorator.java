package structural.decorator;

public class Decorator {
    /*
    When you have variety of add on or toping’s or modifications need to be done for a class, it is not feasible to inherit all the possible combinations, so we can create classes that accepts a base or decorated objects and do some operations on top of the existing class.
     */
}
