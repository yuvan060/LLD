package structural.bridge;

public class Bridge {
    /*
    When we want to decouple the abstraction from the implementation, then we can use bridge deign pattern
    for eg: we have different shapes, and for each shape we are implementing draw method for specific color. To combine all the possibilities we have to create all combinations of classes, instead, we have separate classes for coloring then have this color object in the respective shape class. This reduces code duplication and a better decoupled system.
     */
}
